# 支点 Fulcrum（供应链维权智能查询工具）· 项目框架文档

> 依据《企业供应链维权辅助查询工具 PRD（完整需求文档）》与 `prototype.html` 交互原型整理，作为正式开发落地蓝图。
> 文档版本：v1.0　|　关联文件：`prototype.html`（可交互原型）、`企业供应链维权辅助查询工具 PRD（完整需求文档）.md`（需求）

---

## 1. 项目概述

| 项 | 内容 |
|---|---|
| 产品名称 | 支点 Fulcrum（2026-09-07 定名；文档内功能描述仍沿用"供应链维权智能查询工具"） |
| 产品定位 | 面向职场人、应届生、实习生的**全自动维权渠道智能匹配工具** |
| 核心流程 | 输入公司名称 → 自动挖掘企业供应链 / 海外客户 / 出口信息 → 判断是否拥有欧盟/海外品牌供应链 → 智能输出专属维权方案 |
| 核心价值 | 把复杂的供应链维权变成傻瓜式一键查询（复刻星宇股份学生维权逻辑，产品化、工具化） |
| 产品性质 | 纯信息检索 + AI 分析工具；**不涉及法律咨询、不介入司法判定、不代办维权**；仅整理公开互联网信息 |

### 1.1 三种输出结果（原型已实现三态）

| 状态 | 触发条件 | 输出方案 |
|---|---|---|
| **A. 欧盟/海外供应链** | 命中欧盟客户、出口欧洲、海外营收含欧洲区域、通过欧盟合规认证等 | 海外品牌合规投诉渠道 + 证据清单 + 中英双语投诉模板 |
| **B. 无海外供应链** | 全网无欧洲客户/出口/海外合作公开信息 | 全套国内官方维权渠道 + 证据清单 + 投诉模板 |
| **C. 疑似间接供应链** | 存在间接关联线索（行业报道推断等），未直接证实 | 双线维权建议（国内渠道为主 + 核实后海外施压） |

---

## 2. 页面框架（对应原型结构）

单页面结构：`免责栏 → 标题区 → 查询区 → 分析过程 → 结果区 → 风险提示 → 底部`

```
┌──────────────────────────────────────────┐
│ 🔴 顶部红色固定免责栏（sticky 永久置顶）      │  ← 四条免责声明常驻
├──────────────────────────────────────────┤
│ 标题区：产品名 + 简介 + 核心价值 + 性质标签     │
├──────────────────────────────────────────┤
│ 查询区：公司名输入框 + 「开始智能分析」按钮      │
│         快捷演示按钮（三态示例）               │
├──────────────────────────────────────────┤
│ 分析过程：6 步动画（逐条打勾）                 │
│  ① 检索企业公开信息  ② 获取工商信息           │
│  ③ 解析年报/招股书   ④ AI 识别欧盟客户        │
│  ⑤ 匹配品牌白名单库  ⑥ 生成维权方案           │
├──────────────────────────────────────────┤
│ 结果区（三态动态渲染）：                       │
│  状态头（绿/灰/黄徽章 + 企业名 + 重新查询）     │
│  工商信息自动获取条（可修改）                  │
│  方案正文（品牌/渠道/证据/模板等模块）           │
├──────────────────────────────────────────┤
│ ⚠️ 智能风险提示模块（固定三条，任何结果都展示）   │
├──────────────────────────────────────────┤
│ 底部：产品定位 / 数据来源 / 版本规划            │
└──────────────────────────────────────────┘
```

### 2.1 状态A 结果区模块（高端维权模式）

1. 供应链检测结果（绿色徽章 + 判定依据标签）
2. **工商信息自动获取条**（统一社会信用代码 / 注册地址，带"✏️ 修改"）
3. 合作海外客户名单（品牌卡片：品牌名 + 英文名 + 匹配来源）
4. 对应品牌合规投诉渠道（品牌 × 渠道类型 × 投诉邮箱 × 复制按钮）
5. CSDDD 适配证据清单（勾选 / 全选 / 复制已选）
6. **中英双语投诉邮件模板**（Tab 切换 + 填写表单实时同步 + 一键复制）
7. 维权原理科普（折叠卡片）
8. 风险提示

### 2.2 状态B 结果区模块（普通维权模式）

1. 未检测到海外供应链提示（灰色徽章）
2. 工商信息自动获取条
3. 国内官方维权渠道卡片 ×4：
   - 📞 12333 劳动监察投诉（热线 + 官方网站入口）
   - ⚖️ 劳动仲裁申请（时效说明 + 线上入口指引）
   - 🏛 12345 政务服务热线（热线 + 当地政府网站入口）
   - 🔍 国企/上市公司监管渠道（12388 / 证监会 12386 / 国资委信访 / 国家信访局网址）
4. 国内维权必备证据清单（勾选整理）
5. 可复制投诉文书模板（12333/12345 模板 + 劳动仲裁申请书模板，表单同步）

### 2.3 状态C 结果区模块（疑似间接）

- 黄色警示徽章 + 疑似线索标签
- 双线维权策略建议（国内官方渠道优先 + 核实供应关系后海外施压）
- 风险提示（禁止虚构供应关系投诉）

---

## 3. 核心功能模块设计

### 3.1 企业智能分析模块（核心）

```
输入公司名
   │
   ▼
┌─────────────────────────────────────────────────┐
│ ① 公开信息检索（新闻/官网/招标/中标）               │
│ ② 工商信息获取（信用代码/注册地址，自动填入模板）     │
│ ③ 年报/招股书 PDF 解析（巨潮资讯等）                │
│ ④ AI 识别：欧洲客户、欧盟供应链、出口欧洲、           │
│    国际大厂合作、海外营收欧洲区域                    │
│ ⑤ 匹配预设海外品牌白名单库（奔驰/宝马/大众/奥迪/      │
│    西门子/博世/飞利浦等）                           │
│ ⑥ 判定：有海外供应链 / 无海外供应链 / 疑似间接        │
└─────────────────────────────────────────────────┘
   │
   ▼
输出对应维权方案（状态 A / B / C）
```

**判定规则（PRD §5）：**
- 出现以下任意一项即判定【欧盟供应链】：为奔驰/宝马/大众/欧洲车企供应商；产品出口欧盟/欧洲市场；海外营收包含欧洲区域；通过欧盟合规认证、服务欧洲品牌
- 全网无任何欧洲客户、出口、海外合作公开信息 → 【无海外供应链】
- 存在间接关联线索但未证实 → 【疑似间接供应链】

### 3.2 工商信息自动获取模块（原型新增能力）

- 依据公司名自动获取统一社会信用代码、注册地址（正式产品对接国家企业信用信息公示系统 / 企查查等公开数据源 API）
- 获取结果自动填入全部模板对应位置（eu / domestic / arb 三套模板共享）
- 用户可「✏️ 修改」修正（防止公开数据滞后）
- 未获取到时：提示"可手动补充"，模板保留占位符
- 交互规则：**换公司查询时刷新**工商信息；**同公司重新查询时保留**用户手动修正值

### 3.3 模板表单联动模块（原型核心交互）

用户只需填写表单，模板实时同步，填完直接复制：

```
┌─────────────────────────────────────────────┐
│ 📝 填写表单（按模板分组渲染字段）                 │
│  - 文本/日期/下拉（下拉选项中英对照）              │
│  - 工商字段（auto）不渲染，由系统自动填入          │
└──────────────────┬──────────────────────────┘
                   │ input/change 事件
                   ▼
┌─────────────────────────────────────────────┐
│ 模板渲染引擎（updateTpl）                      │
│  模板 = 固定文本 + {{字段}} 占位符              │
│  - 已填字段 → 替换为用户值                       │
│  - 未填字段 → 保留对应语言兜底占位符（如【姓名】/   │
│    [Name]）                                   │
│  - select 选项按语言取值（中文模板填中文、         │
│    英文模板自动填英文）                          │
│  - 日期字段默认今天                             │
└──────────────────┬──────────────────────────┘
                   ▼
┌─────────────────────────────────────────────┐
│ 中英模板同步更新 → 一键复制（复制当前 Tab 版本）    │
└─────────────────────────────────────────────┘
```

**模板清单（3 套）：**

| 模板 key | 用途 | 语言 | 字段数 |
|---|---|---|---|
| `eu` | 海外品牌供应链合规投诉邮件 | 中文版 + English Version | 10（2 个 auto） |
| `domestic` | 12333/12345 投诉文书 | 中文 | 12（2 个 auto） |
| `arb` | 劳动仲裁申请书 | 中文 | 17（2 个 auto） |

### 3.4 证据清单模块

- 按方案类型提供结构化证据清单（海外 CSDDD 版 10 项 / 国内版 10 项）
- 交互：勾选、全选、取消全选、已选计数、复制已选清单

### 3.5 风险提示模块（强制展示）

所有查询结果固定展示三条（PRD §2 模块4）：
1. 本工具仅基于公开网络信息分析，无法获取企业隐秘供应链，结果仅供参考
2. 海外品牌投诉属于商业合规施压，不具备司法效力，不能替代劳动仲裁
3. 最终维权以中国人社、仲裁、法院官方渠道为准

---

## 4. 数据设计

### 4.1 企业库（原型为演示数据，正式为检索结果）

```json
{
  "星宇股份": {
    "state": "eu",
    "creditCode": "9133020072XXXXXXX1",
    "address": "浙江省宁波市宁海县XX街道XX路1号",
    "brands": [
      { "name": "梅赛德斯-奔驰", "en": "Mercedes-Benz", "src": "年报披露：海外营收含欧洲区域" }
    ],
    "chips": ["产品出口欧盟市场", "欧洲车企一级供应商"]
  }
}
```

正式产品：查询结果实时生成并缓存（TTL 建议 7 天），不预存全量企业。

### 4.2 海外品牌白名单库（预录入静态库）

```json
{
  "name": "梅赛德斯-奔驰",
  "en": "Mercedes-Benz",
  "email": "compliance@mercedes-benz.com",
  "channel": "集团合规举报邮箱"
}
```

V1.0 预录：奔驰、宝马、大众、奥迪、保时捷、西门子、博世、飞利浦等欧盟大型企业。
V1.1：自动更新维护机制。

### 4.3 模板库（内容配置化）

- 模板正文：固定文本 + `{{字段}}` 占位符
- 字段定义：`{ id, label, type, ph, zh, en, auto, options }`
  - `type`: `text | date | textarea | select`
  - `auto: true`：工商字段，表单不渲染、由系统自动填入
  - `options`（select）：对象数组 `{ id, zh, en }`，中英双语、按语言取值
- 违规事实类型选项（双语，6 项）：无故辞退 / 逼迫自离 / 违法调岗 / 拖欠工资 / 超时加班 / 未依法缴纳社保

### 4.4 状态管理（前端）

| 状态 | 说明 |
|---|---|
| `tplState` | 各模板表单填写值（按模板 key 分组），跨查询保留 |
| `currentCompany` / `lastBizCompany` | 当前查询公司 / 上次查询公司（决定工商信息是否刷新） |
| 表单值 | 切 Tab / 重新查询不丢失；「重置本表单」清空并恢复自动值 |

---

## 5. 技术架构建议

### 5.1 总体架构

```
┌────────────────────────────────────────────────────┐
│ 前端（单页应用）                                      │
│ 查询交互 → 分析进度 → 结果三态渲染 → 模板表单联动        │
└──────────────┬─────────────────────────────────────┘
               │ HTTPS / REST API
┌──────────────▼─────────────────────────────────────┐
│ 后端 API 服务                                       │
│  ├ 企业检索聚合服务（新闻/官网/招标/年报/招股书）       │
│  ├ 工商信息服务（信用代码/注册地址）                  │
│  ├ 供应链分析服务（AI 识别 + 品牌库匹配）             │
│  └ 内容服务（模板库 / 白名单库 / 渠道库）             │
└──────────────┬─────────────────────────────────────┘
               │
┌──────────────▼─────────────────────────────────────┐
│ 数据源（全部公开信息）                                 │
│ 公开新闻 · 官网 · 巨潮资讯（年报/招股书 PDF）·          │
│ 招标/中标信息 · 出口数据 · 工商公示数据 · LLM 能力      │
└────────────────────────────────────────────────────┘
```

### 5.2 技术选型建议（可按团队情况替换）

| 层 | 推荐 | 说明 |
|---|---|---|
| 前端 | Vue 3 + Vite（或 React），原生 JS 即可还原原型 | 单页三区块布局，组件化：结果卡片 / 渠道卡 / 模板区 / 证据清单 |
| 样式 | 原生 CSS（原型已定视觉规范：政务蓝 + 顶部红条） | 保持原型视觉体系 |
| 后端 | Node.js（NestJS/Express）或 Python（FastAPI） | 检索聚合 + 内容服务为主 |
| 数据存储 | PostgreSQL（企业库/白名单库/模板库/检索缓存） | 模板与白名单建议配置化（JSON/DB） |
| AI | LLM API（信息抽取、供应链识别、间接推理） | V1.0 关键词+白名单匹配兜底，V1.1 引入 AI 推理 |
| 检索 | 新闻/官网抓取 + 巨潮资讯 PDF 解析管线 | 招股书/年报解析是核心数据源 |

### 5.3 接口设计草案

| 接口 | 方法 | 说明 |
|---|---|---|
| `/api/analyze` | POST | 核心分析：入参 `{ company_name }`，返回状态 + 品牌 + 方案数据 |
| `/api/biz-info` | GET | 工商信息：`?company_name=`，返回信用代码/注册地址 |
| `/api/brand-library` | GET | 品牌白名单库（渠道邮箱/举报链接） |
| `/api/templates` | GET | 模板库（含字段定义，前端渲染表单） |
| `/api/evidence-presets` | GET | 证据清单预设（海外 CSDDD / 国内） |

**`/api/analyze` 响应示例：**

```json
{
  "company_name": "星宇股份",
  "state": "eu",
  "biz_info": { "credit_code": "91330XXXXXXXXXXX", "address": "浙江省宁波市..." },
  "matched_brands": [
    { "name": "梅赛德斯-奔驰", "en": "Mercedes-Benz", "source": "年报披露", "email": "compliance@mercedes-benz.com", "channel": "集团合规举报邮箱" }
  ],
  "verdict_chips": ["产品出口欧盟市场", "欧洲车企一级供应商"],
  "suggestion": "eu"
}
```

---

## 6. 工程目录结构（建议）

```
星宇助手/
├── 企业供应链维权辅助查询工具 PRD（完整需求文档）.md   # 需求文档
├── 项目框架文档.md                                    # 本文档
├── prototype/
│   └── prototype.html                                # 交互原型（演示数据）
├── docs/
│   └── api.md                                        # 接口详细文档（规划）
├── frontend/                                          # 前端工程
│   └── src/
│       ├── pages/Home.vue                            # 单页主结构（免责栏/标题/查询/结果）
│       ├── components/
│       │   ├── TopDisclaimerBar.vue                  # 顶部红色免责栏
│       │   ├── QueryPanel.vue                        # 查询区（输入+演示按钮）
│       │   ├── AnalysisSteps.vue                     # 分析步骤动画
│       │   ├── ResultCard.vue                        # 结果卡片容器（三态）
│       │   ├── BizInfoBar.vue                        # 工商信息自动获取条
│       │   ├── BrandCard.vue / BrandChannelTable.vue # 品牌名单/投诉渠道
│       │   ├── EvidenceList.vue                      # 证据清单
│       │   ├── TemplateSection.vue                   # 模板区（Tab+表单+复制）
│       │   ├── ChannelCard.vue                       # 国内渠道卡片
│       │   └── RiskNotice.vue                        # 智能风险提示
│       ├── stores/tplState.js                        # 模板表单状态
│       └── api/index.js                              # 后端接口封装
├── backend/
│   ├── api/                                          # 路由（analyze/biz-info/...）
│   ├── services/
│   │   ├── searchService.js                          # 公开信息检索聚合
│   │   ├── bizInfoService.js                         # 工商信息获取
│   │   ├── analysisService.js                        # 供应链判定（AI+规则）
│   │   └── pdfParseService.js                        # 年报/招股书解析
│   └── data/
│       ├── brandLibrary.json                         # 海外品牌白名单库
│       └── templates.json                            # 模板库（含字段定义）
└── deploy/                                           # 部署配置（规划）
```

---

## 7. 合规与风控（硬性要求）

1. **永久免责**：顶部红色固定免责栏常驻；底部免责声明；结果页强制展示三条智能风险提示
2. **定位边界**：不构成法律咨询、不提供法律援助；仅整理公开互联网信息
3. **责任声明**：禁止用于恶意诬告、恶意抹黑企业，用户自行承担使用责任
4. **渠道优先级**：明确提示海外投诉仅为商业施压手段，最终以人社、仲裁、法院官方渠道为准
5. **内容真实性**：投诉模板与证据清单引导用户如实填写，禁止虚构供应关系投诉

---

## 8. 版本规划

### V1.0（当前目标）

| 里程碑 | 内容 |
|---|---|
| M1 内容层 | 品牌白名单库、模板库（3 套）、证据清单预设录入 |
| M2 分析层 | 公开信息检索 + 工商信息获取 + 关键词判定（三态） |
| M3 前端 | 还原原型全部页面结构与交互（模板表单联动、复制、证据勾选） |
| M4 联调 | 前端接入 `/api/analyze` 等接口，替换模拟数据 |

### V1.1 迭代

- IPO 企业 ESG 投诉通道
- 间接供应链 AI 推理（LLM 推断供应关系）
- 海外合规邮箱库自动更新
- 渠道网站/邮箱可配置化运营后台

---

## 9. 原型 → 正式实现差距清单

| 原型现状 | 正式实现 |
|---|---|
| `MOCK_COMPANIES` 内置演示数据（星宇股份/宁德时代） | 后端实时检索 + 结果缓存 |
| 品牌邮箱为演示值（compliance@mercedes-benz.com 等） | 白名单库预录入 + 人工核验 + V1.1 自动更新 |
| 工商信息为内置模拟 | 对接国家企业信用信息公示系统 / 工商数据 API |
| 分析步骤为前端动画 | 后端真实检索任务 + 进度推送（轮询/SSE） |
| 模板/字段定义在 JS 常量中 | 配置化（模板库表/JSON），运营可维护 |
| 无账号体系 | V1.0 可无账号（工具型），后续按需加 |

---

## 10. 关键交互验收清单（对照原型）

- [ ] 输入公司名 → 6 步分析动画 → 正确状态结果
- [ ] 三种状态（A/B/C）均可触达（演示按钮 + 关键词）
- [ ] 工商信息自动获取条显示/修改/刷新逻辑正确
- [ ] 表单填写 → 中英模板实时同步 → 一键复制可用
- [ ] 下拉选项目录中英对照，英文模板无中文混入
- [ ] 未填字段保留占位符、已填字段正确替换
- [ ] 证据清单勾选/全选/复制已选
- [ ] 风险提示与免责栏任何结果下均展示
- [ ] 换公司刷新工商信息、同公司保留用户修改
